# Choose one email provider. Prefer SendGrid in Firebase Functions.
#
# For SendGrid:
SENDGRID_API_KEY=
EMAIL_FROM="Innova Hack 2025 <noreply@yourdomain.com>"
#
# For SMTP (fallback if no SendGrid):
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Innova Hack 2025 <noreply@yourdomain.com>"
#
# Base URL for links inside the email
PUBLIC_SITE_URL="https://innovahack2025.web.app"
# Registration ID prefix
REG_PREFIX="INNOVA2025-"
